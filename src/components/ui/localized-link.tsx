/**
 * Locale-aware link. Re-exports next-intl's navigation `Link`, which applies
 * the active locale with the as-needed prefix rule automatically.
 */
export { Link as LocalizedLink } from '@/i18n/navigation';
